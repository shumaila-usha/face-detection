# face-detection
A real-time Face Detection System built with Python and OpenCV. The project uses a webcam to detect and count multiple faces, labels each detected face, draws bounding boxes, and saves detected-face images with timestamps. This project demonstrates practical Computer Vision and AI/ML concepts.
